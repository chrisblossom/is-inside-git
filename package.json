{
  "name": "is-inside-git",
  "version": "1.0.0",
  "description": "Check if a file or directory is inside a git repository",
  "keywords": [
    "is",
    "git",
    "repository",
    "check",
    "is git",
    "is-git"
  ],
  "license": "MIT",
  "private": false,
  "author": "Chris Blossom",
  "engines": {
    "node": ">=8.9.0"
  },
  "files": [
    "dist/"
  ],
  "main": "dist/is-inside-git.js",
  "types": "dist/is-inside-git.d.ts",
  "scripts": {
    "build": "backtrack build --production",
    "clean": "backtrack clean",
    "dev": "backtrack dev --development",
    "format": "backtrack format",
    "git-pre-commit": "backtrack git-pre-commit",
    "git-pre-push": "backtrack git-pre-push",
    "lint": "backtrack lint",
    "lint.fix": "backtrack lint.fix",
    "prepublishOnly": "backtrack prepublishOnly",
    "release": "backtrack release",
    "test": "backtrack test",
    "test.ci": "backtrack test.ci",
    "test.ci-pretest": "backtrack test.ci-pretest",
    "test.update": "jest --update-snapshot",
    "test.watch": "jest --watch",
    "typescript": "backtrack typescript"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.10",
    "temp-sandbox": "^3.0.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "@backtrack/core": "^0.2.1",
    "@backtrack/preset-node": "^0.5.0",
    "execa": "^2.0.0-alpha.0",
    "path-type": "^4.0.0"
  }
}
